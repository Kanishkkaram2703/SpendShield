# v2 Research Baseline

This directory contains the fixed majority-class reference and transparent
standard-library Gaussian Naive Bayes evaluation for dataset v2.

The protocol uses train-only fitting and preprocessing, validation for
research comparison, and one final held-out temporal test evaluation. The
metrics classify synthetic generator scenarios only. They are not fraud,
financial-crime, production, prevention, savings, or risk-reduction metrics.
